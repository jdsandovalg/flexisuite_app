name: backend
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^2.2.0
  shelf_cors_headers: ^0.3.0
  postgres: ^3.5.0
  dotenv: ^5.0.0

dependencies:
  supabase_flutter: ^1.4.0
  flutter:
    sdk: flutter
